import React from 'react';
import classes from './PhotoCells.css';

const photoCells = () => 
    <div className={ classes.PhotoCells }>
        <div className={ classes.PhotoCell }></div>
        <div className={ classes.PhotoCell }></div>
        <div className={ classes.PhotoCell }></div>
        <div className={ classes.PhotoCell }></div>
        <div className={ classes.PhotoCell }></div>
    </div>


export default photoCells;